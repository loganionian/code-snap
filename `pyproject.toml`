[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "code-snap"
version = "0.1.0"
description = "A CLI tool that captures and generates code snippets."
authors = [{name = "Your Name", email = "your.email@example.com"}]
dependencies = ["openai"]